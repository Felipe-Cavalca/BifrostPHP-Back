# Dockerfile for Nginx serving the PHP application
FROM nginx:alpine

# Copia a configuração personalizada do Nginx
COPY nginx.conf /etc/nginx/nginx.conf

# Exponha a porta HTTP
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
